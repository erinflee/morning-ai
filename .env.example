# Copy to .env and fill in. Never commit .env.
# Groq: a single API key powers every role (two models on one account).

GROQ_API_KEY1=

# Optional: higher GitHub API rate limits for fetch_github.py
GITHUB_TOKEN=
